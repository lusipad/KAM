# 2026-03-25 Autonomy Optimization Report

## 结论

这轮没有背离初衷，反而更接近了。

当前系统已经能用真实 `git worktree` + 真实检查命令做自治开发评估，并且可以根据失败样本反向改造系统本身。核心目标不再是“把更多页面堆出来”，而是持续提高三件事：

- AI 自主完成率
- 打断率
- 已完成工作的成功率

2026 年 3 月 25 日的第三轮稳定化战役已经把 10/10 个任务全部在首轮做完，`autonomyCompletionRate = 100%`，`interruptionRate = 0%`，`successRate = 100%`，`averageIterations = 1.0`。

## 战役结果

| 批次 | Campaign ID | 日期 | 样本数 | 自主完成率 | 打断率 | 成功率 | 平均轮次 | 结果 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 基线战役 | `campaign-20260325-114341-4769c1` | 2026-03-25 | 10 | 90% | 0% | 90% | 1.3 | 1 个失败 |
| 改进后战役 | `campaign-20260325-131009-f65d4b` | 2026-03-25 | 10 | 100% | 0% | 100% | 1.8 | 全部完成 |
| 稳定化战役 | `campaign-20260325-145435-0ecc79` | 2026-03-25 | 10 | 100% | 0% | 100% | 1.0 | 全部首轮完成 |

改进前后对比：

- 自主完成率：`90% -> 100%`，提升 `10` 个百分点
- 成功率：`90% -> 100%`，提升 `10` 个百分点
- 打断率：`0% -> 0%`，保持不变
- 平均轮次：`1.3 -> 1.8 -> 1.0`

第二轮平均轮次变高不是退步。改进前很多失败来自 worktree 运行时缺失，Agent 在错误的环境问题上浪费轮次甚至直接失败；改进后，系统先把运行环境补齐，Agent 能进入真实的代码修正与检查闭环，所以更多任务会在第 2 轮完成，而不是在第 1 轮因为环境缺件而终止。

第三轮把平均轮次压回到 `1.0`，说明剩余高频失败已经不是任务本身，而是评估系统里的误报。修掉这层误报后，战役指标才真正回到“AI 是否能自己完成任务”的核心问题上。

## 10 个任务结果

以下列表以第三轮稳定化战役 `campaign-20260325-145435-0ecc79` 为准：

1. `README 自治快速上手补强`：完成，`1/2` 轮
2. `架构文档补自治样本口径`：完成，`1/2` 轮
3. `自治面板显示样本量提示`：完成，`1/2` 轮
4. `自治面板突出最近失败原因`：完成，`1/2` 轮
5. `E2E 覆盖自治标签页骨架`：完成，`1/2` 轮
6. `后端自治指标补充失败检查摘要`：完成，`1/2` 轮
7. `自治文档补操作手册`：完成，`1/2` 轮
8. `Backlog 明确自治优化优先级`：完成，`1/3` 轮
9. `README 补优化战役脚本说明`：完成，`1/3` 轮
10. `自治单测补战役指标稳定性检查`：完成，`1/3` 轮

## 这轮系统改动

### 1. 修掉 Windows 取消清理竞争

提交：`2702245` `Fix Windows run cleanup race`

作用：

- 打断自治会话后，等待 run 线程真正收口
- 释放 `stdout/stderr` 日志句柄
- 避免后续测试或下一轮自治被文件锁卡住

### 2. 给 worktree 自动补运行时

提交：`9e1a37c` `Hydrate worktree runtime for autonomy checks`

作用：

- 在自治 worktree 中自动挂接 `app/node_modules`
- 在自治 worktree 中自动挂接仓库根目录 `.venv`
- 让 `dogfood` 检查真的跑在当前 worktree，而不是误跑主仓库
- 优化战役脚本和 dogfood 默认检查都优先使用稳定 Python 路径

这是本轮最关键的改动。基线战役里唯一失败样本，本质上不是任务理解错，而是 worktree 里缺少可执行运行时，导致 `eslint`、`tsc`、`playwright` 不可用。补齐这层后，第二轮 10 个任务全部完成。

### 3. 把 worktree fallback 从隐性误报改成显式状态

提交：`e62174c` `Report worktree fallback status in runs`

作用：

- 在 run metadata 里显式记录 `worktreeReady`
- 记录 `worktreeReason`，说明为什么回退
- 记录 `runtimeHydration`，说明运行时是否可用
- 把单测从“强假设必须创建 worktree”改成“验证系统是否正确报告 worktree 状态”

这次改动直接消除了第二轮里 `Backend unit` 的伪失败，使第三轮战役回到真实任务完成率的评估。

## 高频失败与解释

基线战役的高频失败项：

- `App lint`: 4 次
- `App build`: 4 次
- `App e2e`: 4 次

改进后战役的高频失败项：

- `Backend unit`: 8 次

稳定化战役的高频失败项：

- 无

解释：

- 第一轮的主要损耗来自环境缺件，属于系统问题
- 第二轮的主要损耗来自评估系统里的误报，具体是 worktree fallback 被单测当成错误
- 第三轮已经把这类误报清掉，指标变成了纯净的任务完成结果

换句话说，当前主要矛盾已经从“系统起不来”，切换到“系统能稳定完成，但还缺更强的长期自治与验收能力”。

## 是否偏离初衷

没有。

反而是把“做产品界面”重新拉回了“做自治开发系统”：

- 关注点从展示层扩展，收敛回了自治闭环
- 衡量方式从主观感受，收敛回了可重复战役与硬指标
- 改动方式从继续堆功能，收敛回了提升完成率与成功率的系统工程

如果按这条线继续推进，后续版本应该优先优化自治效率、失败画像与长期运行能力，而不是继续扩大非核心 UI 面积。

## 下一轮建议

### Now

- 做 campaign history：把多轮战役结果沉淀成可对比时间序列，而不是只看单次报告
- 引入“验收 Agent”：区分规则检查通过和语义目标真正达成
- 给成功 worktree 增加 promotion 路径，输出可审阅 patch / PR 输入物

### Next

- 支持长时间自治调度：定时跑战役、失败后自动续样本、按失败画像自动调整策略
- 在 UI 中展示战役趋势：完成率、打断率、成功率、平均轮次、top failed checks
- 让自治会话支持失败后继续新开样本，而不是只保留单一终态

## 产物位置

- 基线战役原始报告：
  - `storage/campaigns/campaign-20260325-114341-4769c1/report.json`
  - `storage/campaigns/campaign-20260325-114341-4769c1/report.md`
- 改进后战役原始报告：
  - `storage/campaigns/campaign-20260325-131009-f65d4b/report.json`
  - `storage/campaigns/campaign-20260325-131009-f65d4b/report.md`
- 稳定化战役原始报告：
  - `storage/campaigns/campaign-20260325-145435-0ecc79/report.json`
  - `storage/campaigns/campaign-20260325-145435-0ecc79/report.md`
